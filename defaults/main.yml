---
mariadb_user: "{{ __mariadb_user }}"
mariadb_group: "{{ __mariadb_group }}"
mariadb_log_dir: "{{ __mariadb_log_dir }}"
mariadb_log_file: "{{ __mariadb_log_file }}"
mariadb_db_dir: "{{ __mariadb_db_dir }}"
mariadb_service: "{{ __mariadb_service }}"
mariadb_package: "{{ __mariadb_package }}"
mariadb_ansible_packages: "{{ __mariadb_ansible_packages }}"
mariadb_conf_dir: "{{ __mariadb_conf_dir }}"
mariadb_conf_file: "{{ __mariadb_conf_file }}"
mariadb_config: ""
mariadb_flags: ""
mariadb_port: 3306
mariadb_host: 127.0.0.1
mariadb_root_password: ""
mariadb_my_cnf_files: []
mariadb_family: "{{ __mariadb_family }}"
